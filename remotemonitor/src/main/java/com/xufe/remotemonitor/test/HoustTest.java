package com.xufe.remotemonitor.test;

import com.xufe.remotemonitor.mapper.IAdminMapper;
import com.xufe.remotemonitor.services.IAdminService;
import com.xufe.remotemonitor.services.impl.AdminServiceImpl;

import java.text.ParseException;
import java.text.SimpleDateFormat;
import java.time.LocalDate;
import java.time.LocalTime;
import java.time.format.DateTimeFormatter;
import java.util.Date;
import java.util.Random;

public class HoustTest {
    public static void main(String[] args) {

//        LocalDate date = LocalDate.now(); // get the current date
//        DateTimeFormatter formatter = DateTimeFormatter.ofPattern("yyyy-MM-dd");
//        String dbDay = date.format(formatter);
////        String time = "2019-07-23";
//        SimpleDateFormat simpleDateFormat = new SimpleDateFormat("yyyy-MM-dd");
//        Date dateTime = null;
//
//        try {
//            dateTime = simpleDateFormat.parse(dbDay);
//        } catch (ParseException e) {
//            e.printStackTrace();
//        }
//        System.out.println(dateTime);
//        System.out.println("1111==="+date.format(formatter));



        //获取当前日期及时间
//        LocalDate date = LocalDate.now(); // get the current date
//        DateTimeFormatter formatter = DateTimeFormatter.ofPattern("yyyy-MM-dd");
//        System.out.println("1111==="+date.format(formatter));
//
//        LocalTime time = LocalTime.now(); // get the current time
//        DateTimeFormatter timeFormatter = DateTimeFormatter.ofPattern("HH:mm");
//        System.out.println("2222==="+time.format(timeFormatter));


//        要获取一个[x,y]的double类型的随机数 | 左闭右闭[]
//        double x=20.0;
//        double y=40.0;
//        double d = x + Math.random() * y % (y - x + 1);
//        String format = String.format("%.1f", d);
//        System.out.println(format);
    }
}
